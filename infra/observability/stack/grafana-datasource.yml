apiVersion: 1
datasources:
  - name: Prometheus
    # Fixed uid so dashboards/resops.json can reference it deterministically
    # instead of depending on whatever Grafana generates at first boot.
    uid: resops-prom
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
